import { SearchByTenantTypePipe } from './search-by-tenant-type.pipe';

describe('SearchByTenantTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByTenantTypePipe();
    expect(pipe).toBeTruthy();
  });
});
